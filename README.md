I ran 3 algorithms which are 
1) Logistic Regression
2) Decision Tree
3) Random Forest
before I can choose the final model based on the performance.

Therefore, I seperated these into differet R file. The first 2 are in the (3) model improvement section folder where the Random forest is in (2) model fitting secion folder. Each code can be ran independently.

The (1) problem description section folder contained the task that I did on initial data summary and simple visualisations of the data

The ALL3_algorithms_result_plots folder contained the result plot which I did not manage to fit it into the report.


